export default class EventManager {
    constructor({ componentEmitter, socketClient }) {
        this.componentEmitter = componentEmitter 
        this.socketClient = socketClient
    }

    joinRoomAndAwaitForMessages(data){

    }
}